# 무한 수열

## 출처

* https://www.acmicpc.net/problem/1351

## 문제

* 무한 수열 A는 다음과 같다.

```
A0 = 1
Ai = A⌊i/P⌋ + A⌊i/Q⌋ (i ≥ 1)
```

* N, P와 Q가 주어질 때, AN을 구하는 프로그램을 작성하시오.

## 입력

* 첫째 줄에 3개의 정수 N, P, Q가 주어진다.

## 출력

* 첫째 줄에 AN을 출력한다.

## 제한

* 0 ≤ N ≤ 10^12
* 2 ≤ P, Q ≤ 10^9

## 입력 예제 1

```
7 2 3
```

## 출력 예제 1

```
7
```

## 풀이

* 문제 핵심 아이디어

* 수열 정의:

```
A(0) = 1

A(n) = A(n / P) + A(n / Q)
```

👉 N이 최대 10¹²라서
IntArray(N+1) 같은 건 절대 불가 ❌

* 대신, 실제로 필요한 값만 계산

* 이미 계산한 값은 저장
→ HashMap<Long, Long> + 재귀

* 나머지는 dfs를 활용하는 재귀의 방식으로 풀이하면 된다.

## 소스코드

```kotlin
package dynamicprogramming

import java.io.BufferedReader
import java.io.InputStreamReader
import java.util.StringTokenizer
import java.util.HashMap

private lateinit var memo: HashMap<Long, Long>
private var P = 0L
private var Q = 0L

fun dfs(n: Long): Long {
    if (n == 0L) return 1L
    if (memo.containsKey(n)) return memo[n]!!

    val value = dfs(n / P) + dfs(n / Q)
    memo[n] = value
    return value
}

fun main() = with(BufferedReader(InputStreamReader(System.`in`))) {
    val st = StringTokenizer(readLine())
    val N = st.nextToken().toLong()
    P = st.nextToken().toLong()
    Q = st.nextToken().toLong()

    memo = HashMap()
    memo[0L] = 1L

    println(dfs(N))
}
```
